# 验证记录与已知问题

最后更新时间：2026-04-04  
适用版本：当前工作区源码 + 当前发布目录 `dist\win-x64`  
文档类型：活文档

## 1. 最近一次验证结论

最近一次验证时间：`2026-04-04`

已确认：

- `dotnet test` 通过
- 当前自动化测试总数：`12`
- 发布版存在且可执行文件已生成
- 当前标准发布目录：[dist\win-x64](C:\Users\wik\Desktop\storescp工具\dist\win-x64)

最近一次测试命令：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' test 'C:\Users\wik\Desktop\storescp工具\StorescpTool.sln' --no-restore
```

## 2. 已覆盖的验证内容

### 2.1 单元与组件级

已覆盖：

- 默认配置值
- 监听 IP 选择逻辑
- 网络诊断服务基础行为
- 接收记录持久化
- 接收会话持久化
- 诊断包导出

### 2.2 集成级

已覆盖：

- 本机 `StoreSCP -> C-STORE 自测 -> 文件落盘`

对应测试：

- [StoreScpIntegrationTests.cs](C:\Users\wik\Desktop\storescp工具\tests\StorescpTool.Tests\StoreScpIntegrationTests.cs)

## 3. 当前尚未完成的验证

以下验证仍未完成，是后续最重要的风险点：

1. 真实 CT 现场联调验证
2. 第三方 DICOM 模拟器兼容性验证
3. 不同传输语法和异常中断场景验证
4. Windows 防火墙、端口占用、多网卡现场验证

## 4. 已知运行行为

### 4.1 运行时配置会覆盖代码默认值

当前默认值在：

- [AppConfig.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Core\Models\AppConfig.cs)
- [EchoSettings.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Core\Models\EchoSettings.cs)

但一旦运行目录下生成：

```text
config\appsettings.json
```

程序后续将优先加载该配置，而不是重新采用代码默认值。

### 4.2 正在运行时发布到同目录可能失败

如果 `StorescpTool.App.exe` 正在运行，直接发布到同一个输出目录可能出现文件锁定错误。

建议做法：

1. 关闭正在运行的程序
2. 再发布

或：

1. 先发布到新目录
2. 再整目录替换 `dist\win-x64`

### 4.3 当前 shell 里可能没有 dotnet PATH

建议统一使用完整路径：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' ...
```

### 4.4 当前目录已接入 Git

当前目录已经初始化 Git，并已推送远端：

- 默认分支：`main`
- 远端仓库：`git@github.com:kuangliu774392519/storeSCP_tool.git`

后续交接时可以直接使用：

- `git status`
- `git log`
- `git diff`

### 4.5 PowerShell 里部分中文显示可能乱码

在某些命令输出中，中文可能因为终端编码显示异常；目前这更像是终端显示问题，不影响源码编译和程序运行。

## 5. 当前默认参数基线

当前默认值基线如下：

- `Local AE Title = RC120`
- `Listen Port = 5678`
- `Echo Calling AE = RC120`
- `Echo Called AE = CT_AE`
- `Listen IP` 首次启动时会自动选择本机更优先的 IPv4

## 6. 当前发布版状态

当前标准发布版：

- [StorescpTool.App.exe](C:\Users\wik\Desktop\storescp工具\dist\win-x64\StorescpTool.App.exe)

当前观察到的发布目录状态：

- 该目录下目前没有旧的运行时配置残留
- 因此首次启动应显示最新默认参数

## 7. 后续验证建议

如果继续开发，建议每轮至少做以下回归：

1. 运行 `dotnet test`
2. 启动发布版
3. 启动 `StoreSCP`
4. 执行本机 `C-STORE` 自测
5. 检查接收记录、会话统计、日志文件是否更新
