# 开发环境与常用命令

最后更新时间：2026-04-04  
适用版本：当前工作区源码 + 当前发布目录 `dist\win-x64`  
文档类型：活文档

## 1. 当前开发环境

- 操作系统：Windows
- Shell：PowerShell
- 解决方案文件：[StorescpTool.sln](C:\Users\wik\Desktop\storescp工具\StorescpTool.sln)
- `.NET SDK` 路径：`C:\Program Files\dotnet\dotnet.exe`

注意：

- 当前 shell 环境中不一定能直接识别 `dotnet`
- 建议命令统一写成：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' <command>
```

## 2. 常用运行命令

开发运行：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' run --project 'C:\Users\wik\Desktop\storescp工具\src\StorescpTool.App\StorescpTool.App.csproj'
```

测试：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' test 'C:\Users\wik\Desktop\storescp工具\StorescpTool.sln' --no-restore
```

标准发布：

```powershell
& 'C:\Program Files\dotnet\dotnet.exe' publish 'C:\Users\wik\Desktop\storescp工具\src\StorescpTool.App\StorescpTool.App.csproj' -c Release -r win-x64 --self-contained true -p:PublishSingleFile=false -p:IncludeNativeLibrariesForSelfExtract=true -o 'C:\Users\wik\Desktop\storescp工具\dist\win-x64'
```

脚本发布：

```powershell
& 'C:\Users\wik\Desktop\storescp工具\scripts\Publish-StorescpTool.ps1'
```

## 3. 当前可执行文件路径

- 标准发布版：[StorescpTool.App.exe](C:\Users\wik\Desktop\storescp工具\dist\win-x64\StorescpTool.App.exe)
- 解决方案入口：[StorescpTool.sln](C:\Users\wik\Desktop\storescp工具\StorescpTool.sln)

## 4. 运行时目录结构

相对于应用程序目录，当前主要运行时目录如下：

```text
config\
  appsettings.json

data\
  logs\
  received\
  state\
    receive_records.json
    receive_sessions.json
  export\
```

## 5. 运行时配置路径

运行时配置由 [AppConfigService.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Infrastructure\Config\AppConfigService.cs) 管理，真实配置文件路径是：

```text
<应用目录>\config\appsettings.json
```

重要行为：

- 如果该文件不存在，程序会按 [AppConfig.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Core\Models\AppConfig.cs) 默认值创建
- 如果该文件存在，它会覆盖代码默认值

## 6. 当前默认参数

来自 [AppConfig.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Core\Models\AppConfig.cs) 和 [EchoSettings.cs](C:\Users\wik\Desktop\storescp工具\src\StorescpTool.Core\Models\EchoSettings.cs)：

- `LocalAeTitle = RC120`
- `ListenPort = 5678`
- `ListenIp = ""`，运行时由 `ListenIpSelector` 自动补合适的本机 IPv4
- `Echo.CallingAeTitle = RC120`
- `Echo.CalledAeTitle = CT_AE`

## 7. 发布时常见问题

### 7.1 文件被锁

如果当前程序正在运行，发布到同一个输出目录时可能出现 DLL 或 EXE 被锁。

处理方式：

1. 先关闭正在运行的 `StorescpTool.App.exe`
2. 再执行标准发布

如果不方便关闭正在运行的实例，可先发布到新目录，再整目录替换 `dist\win-x64`。

### 7.2 PATH 中没有 dotnet

症状：

- `dotnet` 命令提示未识别

处理方式：

- 使用完整路径 `C:\Program Files\dotnet\dotnet.exe`

## 8. 版本管理说明

当前目录不是 Git 仓库：

- `git status` 会失败
- 没有分支、提交历史和 PR 流程基线

如果未来准备长期维护，建议尽快初始化 Git 仓库。
